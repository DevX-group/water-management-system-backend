spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

cloudinary:
  cloud_name: df87pqbem
  api_key: 626896215766676
  api_secret: eHUnG-RbDliFhdR-TkW9iPHrs6s

payhere:
  merchant_id: ${PAYHERE_MERCHANT_ID} 
  merchant_secret: ${PAYHERE_MERCHANT_SECRET}
  return_url: ${PAYHERE_RETURN_URL}
  cancel_url: ${PAYHERE_CANCEL_URL}
  notify_url: ${PAYHERE_NOTIFY_URL}

text-lk:
  api-token: ${TEXT_LK_API_TOKEN}
  sender-id: ${TEXT_LK_SENDER_ID}
  api-endpoint: ${TEXT_LK_API_ENDPOINT}


server:
  port: ${PORT:8081}
